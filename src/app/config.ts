export class Url{
    url='http://localhost:3000/api/';
    /**
     * metodo para obtener la del webservice
     * @returns la dirección del webservice
     */
    getUrl(){
        return this.url;
    }
}